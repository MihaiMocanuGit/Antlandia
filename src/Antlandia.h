#pragma once
#include "World/ChunkMap.h"
#include "AppLogic/AppLogic.h"

namespace Antlandia
{
/// \brief Starts the demo world
/// \param sizeX the width of the window and world
/// \param sizeY the height of the window and world
void startApp(unsigned sizeX, unsigned sizeY);
}
